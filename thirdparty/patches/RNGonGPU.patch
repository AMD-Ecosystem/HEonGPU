diff --git a/CMakeLists.txt b/CMakeLists.txt
index efb7ca3..e1d43d4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -4,7 +4,12 @@
 # Developer: Alişah Özcan
 
 cmake_minimum_required(VERSION 3.26)
-project(RNGonGPU VERSION 1.0 LANGUAGES C CXX CUDA ASM)
+option(USE_HIP "Build with HIP for AMD GPUs" OFF)
+if(USE_HIP)
+    project(RNGonGPU VERSION 1.0 LANGUAGES C CXX HIP)
+else()
+    project(RNGonGPU VERSION 1.0 LANGUAGES C CXX CUDA ASM)
+endif()
 
 if(NOT CMAKE_BUILD_TYPE OR CMAKE_BUILD_TYPE STREQUAL "")
   set(CMAKE_BUILD_TYPE Release CACHE STRING "Build type (Debug or Release)" )
@@ -28,9 +33,15 @@ set(CMAKE_CUDA_FLAGS_DEBUG   "${CMAKE_CUDA_FLAGS_DEBUG} -g")
 set(CMAKE_CUDA_FLAGS_RELEASE "${CMAKE_CUDA_FLAGS_RELEASE} -O3")
 
 find_package(OpenSSL REQUIRED)
-find_package(CUDAToolkit REQUIRED)
+if(NOT USE_HIP)
+    find_package(CUDAToolkit REQUIRED)
+endif()
 
-if(NOT DEFINED CMAKE_CUDA_ARCHITECTURES OR CMAKE_CUDA_ARCHITECTURES STREQUAL "")
+if(USE_HIP)
+    if(NOT DEFINED CMAKE_HIP_ARCHITECTURES OR CMAKE_HIP_ARCHITECTURES STREQUAL "")
+        set(CMAKE_HIP_ARCHITECTURES gfx90a CACHE STRING "HIP GPU architectures" FORCE)
+    endif()
+elseif(NOT DEFINED CMAKE_CUDA_ARCHITECTURES OR CMAKE_CUDA_ARCHITECTURES STREQUAL "")
     set(CMAKE_CUDA_ARCHITECTURES 80;86;89;90 CACHE STRING "CUDA GPU architectures" FORCE)
 endif()
 message(STATUS "Setting CMAKE_CUDA_ARCHITECTURES to ${CMAKE_CUDA_ARCHITECTURES} for RNGonGPU")
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index ad6e5b3..54fd2f5 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -11,6 +11,10 @@ set(LIB_SOURCE_FILES
     lib/rand_cuda/cuda_rng_kernels.cu
 )
 
+if(USE_HIP)
+    set_source_files_properties(${LIB_SOURCE_FILES} PROPERTIES LANGUAGE HIP)
+endif()
+
 add_library(rngongpu STATIC ${LIB_SOURCE_FILES})
 add_library(RNGonGPU::rngongpu ALIAS rngongpu)
 if(CMAKE_CXX_COMPILE_FEATURES)
@@ -52,18 +56,25 @@ set_target_properties(rngongpu PROPERTIES
 
 target_link_libraries(rngongpu PUBLIC 
     GPUNTT::ntt
-    CUDA::curand
+    $<IF:$<BOOL:${USE_HIP}>,hip::hiprand,CUDA::curand>
     OpenSSL::SSL
     OpenSSL::Crypto
 )
 
-set_target_properties(rngongpu PROPERTIES
+if(USE_HIP)
+  set_target_properties(rngongpu PROPERTIES
+        POSITION_INDEPENDENT_CODE ON
+        HIP_ARCHITECTURES "${CMAKE_HIP_ARCHITECTURES}"
+  )
+else()
+  set_target_properties(rngongpu PROPERTIES
         CUDA_SEPARABLE_COMPILATION ON
         POSITION_INDEPENDENT_CODE ON
         CUDA_RESOLVE_DEVICE_SYMBOLS ON
         CUDA_RUNTIME_LIBRARY Static
         CUDA_ARCHITECTURES "${CMAKE_CUDA_ARCHITECTURES}"
 )
+endif()
 
 target_include_directories(
     rngongpu
